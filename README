This is my Vim config.

cd
git clone git@github.com:romainl/dotvim.git .vim
echo "runtime vimrc" > .vimrc

git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

udo apt install universal-ctags

in vim :PluginUpdate
